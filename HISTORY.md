## 2026-05-02 (토)
### 변경 사항
- **24004 에러 매핑 수정**
  - 넷마블 API 에러 코드 24004 처리 시 서버가 제공하는 실제 errorMessage를 우선 사용하도록 수정
  - 기존: 고정 메시지 "이미 등록된 코드입니다." 반환
  - 변경: err.errorMessage 우선 노출, 없을 때만 기본 메시지 사용
  - 파일: lib/netmarble.ts
- **24002, 24003 에러 메시지도 서버 메시지 노출하도록 정리**
  - 24002(잘못된 코드), 24003(만료된 코드)도 서버의 errorMessage를 우선 사용하도록 수정
  - 파일: lib/netmarble.ts
- **커뮤니티 코드 중복 제거 로직 추가**
  - GET /codes 응답 시 대소문자 무시 중복 제거 후 저장(community_codes)
  - 중복이 발견되면 KV에 정리된 리스트를 자동 저장하여 리스트 중복 표시 방지
  - 파일: app/api/codes/route.ts
- **Code 모델에 gameCode 필드 추가**
  - CodeSchema에 gameCode?: string (기본값 "monster2") 추가
  - 모든 쿠폰 코드가 게임별로 매핑될 수 있도록 확장
  - 파일: lib/schemas.ts, data/codes.json
- **per-code gameCode 지원 및 기본값 monster2 적용**
  - redeemCoupon 함수가 code별 gameCode를 받도록 수정
  - staticCodes 및 community_codes에서 해당 코드의 gameCode를 조회하여 사용
  - 기본값은 "monster2"로 유지
  - 파일: lib/netmarble.ts, app/api/redeem/route.ts
- **신규 리딤코드 감지 및 관리자 알림 기능 추가**
  - 등록되지 않은 새로운 코드 사용 시 KV에 알림 저장 및 관리자에게 알림 전송
  - 24시간 중복 알림 방지 로직 포함
  - 파일: app/api/redeem/route.ts
- **SPECIALGIFT0430 중복 문제 해결**
  - 커뮤니티 코드 중복 제거 로직으로 자동 정리되도록 개선
- **배포 및 푸시**
  - 리모트 저장소(origin master)에 변경 사항 푸시 및 배포 진행

## 2026-04-30 (목)
### 변경 사항
- **쿠폰 보상 자동 업데이트 기능 추가**
  - 쿠폰 등록 성공 시 넷마블 API 응답에서 상품명을 추출하여 보상 정보로 자동 저장
  - `community_codes`의 보상이 '미확인'인 경우 즉시 업데이트
  - `staticCodes`의 보상이 '미확인'인 경우 KV의 `reward_overrides`를 통해 오버라이드 적용
- **만료된 쿠폰 자동 제거 기능 구현**
  - 쿠폰 등록 시도 중 '만료됨'으로 판명된 코드는 KV의 `expired_codes` 블랙리스트에 추가
  - 쿠폰 목록 조회 시 블랙리스트에 포함된 코드는 자동으로 필터링되어 노출되지 않음
- **쿠폰 제안 중복 방지 로직 강화**
  - 쿠폰 제안 시 이미 등록된 코드이거나 만료된 코드인 경우 사용자에게 알림 제공
  - 대소문자 구분 없이 코드 중복 여부 확인
- **배포 및 푸시**
  - 리모트 저장소(origin master)에 변경 사항 푸시 및 배포 진행


## 2026-04-27 (월)
### 변경 사항
- **메인 페이지 개선**
  - 우측 상단에 '공식 포럼' 및 '관리자 페이지' 링크 추가
  - 공식 포럼 링크: `https://forum.netmarble.com/stardive_ko`
- **쿠폰 보상 상태 저장 로직 수정**
  - 캐시 키에서 날짜 정보를 제거하여 쿠폰 등록 상태('이미 등록됨' 등)가 영구적으로 유지되도록 변경
  - 회원번호 입력 시 즉시 로컬 캐시 정보를 반영하여 상태가 업데이트되도록 `useEffect` 추가
- **배포 및 푸시**
  - 리모트 저장소(origin master)에 변경 사항 푸시 및 배포 대기
- **문서 체계 정리 및 지침 강화**
  - `AGENTS.md`를 중심으로 개발 지침 통합 (소스 최신화, 푸시 필수화 등)
  - 불필요한 `CLAUDE.md` 파일 삭제
  - 세션 시작 시 `git pull --rebase` 수행 방침 명문화
- **관리자 페이지 네비게이션 개선**
  - 관리자 로그인 및 대시보드 상단에 '메인으로 돌아가기' 링크 추가
  - 뒤로가기 버튼 없이도 메인 페이지로 쉽게 이동 가능하도록 개선
- **공식 포럼 URL 수정**
  - 공식 포럼 주소를 최신 정보(`https://forum.netmarble.com/stardive_ko`)로 업데이트
- **보안 감사 수행**
  - 전체 소스 코드 내 민감 정보 하드코딩 여부 점검 (이상 없음)
  - IP 주소 익명화 및 프라이버시 보호 로직 확인 (SHA-256 + Salt 적용 확인)
  - 환경 변수 관리 체계 및 .gitignore 설정 점검 (정상)
  - 관리자 인증 및 무차별 대입 공격 방지 로직 확인 (정상)
